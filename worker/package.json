{
  "name": "markreview-worker",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types src/cloudflare-runtime.d.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "wrangler": "^4.80.0"
  }
}
